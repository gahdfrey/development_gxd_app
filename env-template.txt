# Environment Variables Template for GXD App
# Copy contents to .env.local file

NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production-min-32-chars
NEXTAUTH_URL=http://localhost:3000
DATABASE_URL=postgresql://postgres:password@localhost:5432/gxd_app

# Landing-page contact form (app/api/contact) — sends via SMTP.
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_USER=your gmail
# address, SMTP_PASS=a 16-character Gmail "App Password" (not your normal
# password — generate one at https://myaccount.google.com/apppasswords,
# requires 2-Step Verification to be enabled on the account).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM=your-gmail-address@gmail.com
# Optional: override who receives contact-form submissions (defaults to gessien3@gmail.com)
CONTACT_RECIPIENT_EMAIL=gessien3@gmail.com

# Instructions:
# 1. Copy this file's contents to .env.local
# 2. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Update DATABASE_URL with your PostgreSQL credentials
# 4. For production, update NEXTAUTH_URL to your domain
# 5. Fill in SMTP_* above to enable the landing-page contact form
